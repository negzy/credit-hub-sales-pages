# Database (Postgres — use Neon/Vercel Marketplace URL)
# Get from Vercel: Storage → Neon → connect project, then copy POSTGRES_PRISMA_URL into DATABASE_URL
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Vault: one password to unlock the full database. Share this with paid users.
VAULT_UNLOCK_PASSWORD=changeme
